apiVersion: v1
kind: ConfigMap
metadata:
  name: modern-nginx-config
data:
  default.conf: |
    server {
        listen 80;
        location / {
            default_type text/plain;
            return 200 "[MODERN SYSTEM] You reached the new Microservice!\nPath Hit: $request_uri\n";
        }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: modern-microservice
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: modern-microservice
  template:
    metadata:
      labels:
        app: modern-microservice
    spec:
      containers:
      - name: nginx
        image: {{ .Values.images.nginx }}
        ports:
        - containerPort: {{ .Values.ports.backendTarget }}
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
      volumes:
      - name: nginx-config
        configMap:
          name: modern-nginx-config
---
apiVersion: v1
kind: Service
metadata:
  name: modern-microservice
spec:
  selector:
    app: modern-microservice
  ports:
  - port: {{ .Values.ports.backendService }}
    targetPort: {{ .Values.ports.backendTarget }}